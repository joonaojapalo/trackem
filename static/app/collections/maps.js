// Generated by CoffeeScript 1.10.0
(function() {
  define(["underscore", "backbone", "app/models/map"], function(_, Backbone, Map) {
    var Maps;
    return Maps = Backbone.Collection.extend({
      model: Map,
      url: function() {
        var groupId;
        groupId = this.group.get("id");
        return "/api/groups/" + groupId + "/maps";
      }

      /*initialize: (models, options) ->
      			_.bindAll @, "url"
      			@group = options.group
       */
    });
  });

}).call(this);
